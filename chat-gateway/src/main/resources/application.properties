spring.application.name=chat-gateway
internal.oauth.token-uri=${INTERNAL_OAUTH_TOKEN_URI}
internal.oauth.client-id=${GATEWAY_CLIENT_ID}
internal.oauth.client-secret=${GATEWAY_CLIENT_SECRET}
internal.chat-api.base-url=${CHAT_API_BASE_URL}
internal.chat-message.base-url=${CHAT_MESSAGE_BASE_URL}
